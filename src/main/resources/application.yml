spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "1234"
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 🛠️ 중복되었던 spring: 대문을 없애고 안으로 쏙 정렬했습니다!
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 📷 내가 원하는 로컬 저장 경로
file:
  upload-dir: C:/workspace/Chat/IMAGE/